import { useContext } from "react";
import { GameContext } from "../context/GameContext";

export default function SwitchThemeButton() {
  return (
    <>
      <button className="switchThemeButton">T</button>
    </>
  );
}
